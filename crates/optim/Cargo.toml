[package]
name = "optim"
version = "0.1.0"
edition = "2024"

[dependencies]
rayon = "1.10"
num_cpus = "1.16"
once_cell = "1.20"
tokio = { version = "1.40", features = ["full"] }
futures = "0.3"
async-trait = "0.1"

[dev-dependencies]
criterion = "0.5"
